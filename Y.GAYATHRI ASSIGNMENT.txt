1. SELECT ENAME
   FROM EMP
   WHERE JOB = 'CLERK';

2. SELECT ENAME
   FROM EMP
   WHERE JOB='CLERK' OR 'MANAGER';

3. SELECT GETDATE() 
   AS CURRENTDATEANDTIME;

4. SELECT ENAME
   FROM EMP
   WHERE SAL BETWEEN 500 AND 1000;

5. SELECT ENAME
   FROM EMP
   WHERE HIREDATE >'01 DEC 1982' AND HIREDATE<'31 DEC 1982';

6. SELECT ENAME
   FROM EMP 
   WHERE SAL NOT BETWEEN 1000 AND 2500;

7. SELECT ENAME
   FROM EMP
   WHERE EMPNO IN('800','950','3000','5000');

8. SELECT ENAME
   FROM EMP
   WHERE ENAME LIKE'_L%';

9. SELECT ENAME
   FROM EMP
   WHERE ENAME LIKE'%D';

10.SELECT ENAME
   FROM EMP
   WHERE LEN(ENAME)=5;

11. SELECT ENAME
    FROM EMP
    WHERE ENAME LIKE'T%R';

12. SELECT ENAME
    FROM EMP
    WHERE ENAME LIKE'%A%';

13. SELECT ENAME
    FROM EMP
    WHERE MONTH(HIREDATE) <'19';

14. SELECT EMPNO
    FROM EMP
    WHERE ENAME='JAMES';

15. SELECT SAL
    FROM EMP
    WHERE ENAME='KING';

16. SELECT ENAME
    FROM EMP
    WHERE JOB='PRESIDENT';

17. SELECT ENAME
    FROM EMP
    WHERE JOB='MANAGER';

18. SELECT ENAME
    FROM EMP
    WHERE NOT (COMM= NULL AND COMM=0);

19. SELECT ENAME
    FROM EMP
    WHERE HIREDATE<'1981-04';

20. SELECT SAL
    FROM EMP
    WHERE ENAME IN('KING','BLAKE','FORD','SMITH');

22. SELECT ENAME
    FROM EMP
    WHERE SAL<3500;

23. SELECT *
    FROM EMP
    WHERE JOB='CLERK';

24. SELECT JOB,SAL*12
    FROM EMP;

25. SELECT (SAL*12)+20
    FROM EMP
    WHERE ENAME='FORD';

26. SELECT DISTINCT JOB,SUM(SAL) AS TOTAL
    FROM EMP
    GROUP BY JOB;

27. SELECT MIN(SAL) AS LOWEST
    FROM EMP
    WHERE DEPTNO='10';

28. SELECT AVG(SAL)
    FROM EMP
    WHERE JOB='MANAGER';

29. SELECT SUM(SAL)
    FROM EMP
    WHERE JOB='SALESMAN';

30. SELECT JOB,AVG(SAL)
    FROM EMP
    GROUP BY JOB;

31. SELECT COUNT(*) AS PERSON,JOB
    FROM EMP
    GROUP BY JOB;

32. SELECT MAX(SAL)
    FROM EMP
    WHERE JOB='ANALYST';

33. SELECT MAX(COMM)
    FROM EMP;

34. SELECT COUNT(*)
    FROM DEPT
    WHERE DEPTNO NOT IN(SELECT DEPTNO
                        FROM EMP);
    
    
35. SELECT ENAME
    FROM EMP
    WHERE ENAME LIKE'%L__';
    
36.  SELECT DISTINCT JOB
    FROM EMP;

37.  SELECT ENAME,JOB
    FROM EMP
    WHERE MGR IS NULL;

38.  SELECT E.ENAME,E.JOB
    FROM EMP E,DEPT D
    WHERE D.LOC='NEWYORK' AND COMM>'1000';
 
39. SELECT COUNT(ENAME)
   FROM EMP
   WHERE DEPTNO='30'; 

40. SELECT ENAME
   FROM EMP
   WHERE DEPTNO='30'; 

41. SELECT E.ENAME,D.LOC
   FROM EMP E
   INNER JOIN DEPT D
   WHERE E.DEPTN0


42. SELECT DISTINT ENAME
   FROM EMP
   JOIN
   SELECT DISTINT DNAME
   FROM DEPT

43. SELECT DISTINCT E.EMPNO
    FROM EMP E
    INNER JOIN EMP M
    ON E.MGR=M.EMPNO
    AND E.HIREDATE<M.HIREDATE;


44.  SELECT COUNT(ENAME)
    FROM EMP
    WHERE JOB='MANAGER';


45. SELECT ENAME,JOB
    FROM EMP
    WHERE MGR IS NULL;

46. SELECT ENAME,DEPTNO
    FROM EMP
    WHERE ENAME='SMITH';

47. SELECT E.ENAME,D.LOC
    FROM EMP E,DEPT D
    WHERE D.LOC IS NOT BOSTON;

48. SELECT E.ENAME,D.LOC
    FROM EMP E,DEPT D
    WHERE E.DEPTNO=D.DEPTNO;

49. SELECT ENAME
    FROM EMP
    WHERE SAL < (SELECT SAL
                 FROM EMP
                 WHERE ENAME='MILLER');

50. SELECT *
    FROM EMP
    WHERE DEPTNO='30';

51. SELECT ENAME
    FROM EMP
    WHERE SAL<(SELECT MIN(SAL)
    FROM EMP
    WHERE JOB='MANAGER');

52. SELECT ENAME,DEPTNO
    FROM EMP
    WHERE JOB='ACCOUNTING';

53. SELECT ENAME
    FROM EMP
    WHERE JOB='MANAGER';

54. SELECT CASE 
    WHEN DEPTNO IN ('10','20','30') THEN 'NONSALES'
		   WHEN SAL>1000 THEN SAL
		   ELSE 1000
		   END
		   FROM EMP;



 
    
